<%= current_user.name %>
<% @projects.each do |project| %>

  has <%= @projects.count %> project(s).</br>

  <%= project.name %> has <%= @projects.number_of_collaborators %> collaborators.</br>

<% end %>
